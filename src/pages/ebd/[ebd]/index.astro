---
import Layout from '../../../layouts/Layout.astro';

const {ebd} = Astro.params;
---

<Layout title="CB Atos 29">
  <div class="flex flex-col gap-4">
    <h1 class="font-bold">{ebd}</h1>
    <a href={`/ebd`}>Voltar</a>
    <a href={`/ebd/${ebd}/subscribe`}>Inscreva-se</a>
  </div>
</Layout>